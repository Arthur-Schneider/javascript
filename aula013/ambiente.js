var c =1

/* while(c <= 100){ //Vai repetir enquanto c for menor ou igual a 100
    console.log(`Passo ${c}`)
    c = c + 1 //Soma 1 a cada vez que o bloco repete (poderia ser escrito c++)
} */

/* do{
    console.log(`Passo ${c}`)
    c = c + 1 //Soma 1 a cada vez que o bloco repete (poderia ser escrito c++)
}while(c <= 6) */

for(var c = 1; c <= 6; c++){ //Mesmo exemplo dos acima mas com o FOR
    console.log(`Passo ${c}`)
}